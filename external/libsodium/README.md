This directory intentionally contains only libsodium public headers as a convenience for environments that already provide a runtime libsodium library but not development headers.

For production builds, prefer a system package, vcpkg, Conan, or set SODIUM_INCLUDE_DIR and SODIUM_LIBRARY explicitly.
